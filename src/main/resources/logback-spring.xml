<configuration>
    <!-- Désactiver les logs DEBUG trop fréquents de MongoDB -->
    <logger name="org.mongodb.driver.cluster" level="INFO" />
    <logger name="org.mongodb.driver.connection" level="INFO" />
    <logger name="org.mongodb.driver.operation" level="INFO" />

    <!-- Appender pour la console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} | [%thread] | %-5level | %logger{36} | %msg || trace_id=%X{trace_id} || span_id=%X{span_id}%n</pattern>
        </encoder>
    </appender>

    <!-- Appender OpenTelemetry -->
    <appender name="OTEL" class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} | [%thread] | %-5level | %logger{36} | %msg || trace_id=%X{trace_id} || span_id=%X{span_id}%n</pattern>
        </encoder>
    </appender>

    <!-- Niveau global et association des appenders -->
    <root level="debug">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="OTEL" />
    </root>
</configuration>